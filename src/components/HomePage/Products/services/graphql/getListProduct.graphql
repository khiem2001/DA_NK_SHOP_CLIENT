query getlistProduct($input: GetListProductInput!) {
  getListProduct(input: $input) {
    products {
      _id
      name
      description
      countInStock
      total_like
      price
      image {
        url
      }
    }
    pagination {
      totalPage
      currentPage
      pageSize
    }
    totalItem
  }
}
