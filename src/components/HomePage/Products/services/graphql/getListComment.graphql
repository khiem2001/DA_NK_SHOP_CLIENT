query getListComment($input: ListCommentInput!) {
  listComment(input: $input) {
    data {
      _id
      user {
        avatarId {
          url
        }
        fullName
      }
      message
      createdAt
    }
  }
}
