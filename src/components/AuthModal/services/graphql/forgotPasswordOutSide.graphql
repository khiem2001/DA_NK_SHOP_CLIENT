mutation changePasswordOutSide($password: String!, $sessionId: String!, $confirmPassword: String!, $otp: String!) {
  changePassword(input: { password: $password, sessionId: $sessionId, confirmPassword: $confirmPassword, otp: $otp }) {
    updated
  }
}
